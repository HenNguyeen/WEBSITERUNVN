@model DBStoreSport.Models.Voucher
@{
    ViewBag.Title = "Chi tiết Voucher";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Content/admin-dashboard.css" rel="stylesheet" />

<div class="admin-container">
    <div class="admin-header">
        <h2>🔍 Chi tiết Voucher</h2>
        <div>
            <a href="@Url.Action("Edit", "AdminVoucher", new { id = Model.VoucherID })" class="btn btn-outline-warning btn-sm me-2">
                <i class="fas fa-edit"></i>
                Chỉnh sửa
            </a>
            <a href="@Url.Action("Index", "AdminVoucher")" class="btn btn-outline-secondary btn-sm">
                <i class="fas fa-arrow-left"></i>
                Quay lại
            </a>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        Thông tin Voucher
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-group mb-4">
                                <label class="info-label">ID Voucher:</label>
                                <div class="info-value">@Model.VoucherID</div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-group mb-4">
                                <label class="info-label">Mã Voucher:</label>
                                <div class="info-value">
                                    <span class="voucher-code">@Model.Code</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-group mb-4">
                                <label class="info-label">Loại giảm giá:</label>
                                <div class="info-value">
                                    @if (Model.Type == "Percent")
                                    {
                                        <span class="badge bg-primary">Phần trăm (%)</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning">Số tiền cố định (VNĐ)</span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-group mb-4">
                                <label class="info-label">Giá trị giảm:</label>
                                <div class="info-value">
                                    @if (Model.Type == "Percent")
                                    {
                                        <span class="badge bg-info fs-6">@Model.DiscountValue%</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success fs-6">@Model.DiscountValue.ToString("N0") VNĐ</span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-group mb-4">
                                <label class="info-label">Ngày hết hạn:</label>
                                <div class="info-value">
                                    @if (Model.ExpiryDate < DateTime.Now)
                                    {
                                        <span class="text-danger">
                                            <i class="fas fa-clock me-2"></i>
                                            @Model.ExpiryDate.ToString("dd/MM/yyyy HH:mm")<br>
                                            <small class="text-muted">Đã hết hạn</small>
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="text-success">
                                            <i class="fas fa-calendar-check me-2"></i>
                                            @Model.ExpiryDate.ToString("dd/MM/yyyy HH:mm")<br>
                                            <small class="text-muted">Còn hiệu lực</small>
                                        </span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-group mb-4">
                                <label class="info-label">Trạng thái sử dụng:</label>
                                <div class="info-value">
                                    @if (Model.IsUsed == true)
                                    {
                                        <span class="badge bg-secondary fs-6">
                                            <i class="fas fa-check-circle me-1"></i>
                                            Đã sử dụng
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success fs-6">
                                            <i class="fas fa-clock me-1"></i>
                                            Chưa sử dụng
                                        </span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">
                            <div class="info-group mb-4">
                                <label class="info-label">Trạng thái tổng thể:</label>
                                <div class="info-value">
                                    @if (Model.IsUsed == true)
                                    {
                                        <span class="badge bg-secondary fs-5">
                                            <i class="fas fa-ban me-2"></i>
                                            Voucher đã được sử dụng
                                        </span>
                                    }
                                    else if (Model.ExpiryDate < DateTime.Now)
                                    {
                                        <span class="badge bg-danger fs-5">
                                            <i class="fas fa-exclamation-triangle me-2"></i>
                                            Voucher đã hết hạn
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success fs-5">
                                            <i class="fas fa-check-circle me-2"></i>
                                            Voucher có hiệu lực
                                        </span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-lightbulb me-2"></i>
                        Thông tin bổ sung
                    </h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle me-2"></i>Thông tin voucher:</h6>
                        <ul class="mb-0">
                            <li><strong>Mã:</strong> @Model.Code</li>
                            <li><strong>Loại:</strong> @(Model.Type == "Percent" ? "Phần trăm" : "Số tiền cố định")</li>
                            <li><strong>Giá trị:</strong> @(Model.Type == "Percent" ? Model.DiscountValue + "%" : Model.DiscountValue.ToString("N0") + " VNĐ")</li>
                        </ul>
                    </div>

                    <div class="alert alert-warning">
                        <h6><i class="fas fa-exclamation-triangle me-2"></i>Lưu ý:</h6>
                        <ul class="mb-0">
                            <li>Voucher chỉ có thể sử dụng một lần</li>
                            <li>Voucher sẽ tự động hết hiệu lực sau ngày hết hạn</li>
                            <li>Không thể chỉnh sửa voucher đã sử dụng</li>
                        </ul>
                    </div>

                    <div class="alert alert-success">
                        <h6><i class="fas fa-calendar me-2"></i>Thời gian:</h6>
                        <ul class="mb-0">
                            <li><strong>Ngày tạo:</strong> Không có thông tin</li>
                            <li><strong>Ngày hết hạn:</strong> @Model.ExpiryDate.ToString("dd/MM/yyyy")</li>
                            <li>
                                <strong>Còn lại:</strong>
                                @if (Model.ExpiryDate > DateTime.Now)
                                {
                                    var timeLeft = Model.ExpiryDate - DateTime.Now;
                                    <span class="text-success">@timeLeft.Days ngày @timeLeft.Hours giờ</span>
                                }
                                else
                                {
                                    <span class="text-danger">Đã hết hạn</span>
                                }
                            </li>
                        </ul>
                    </div>
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="@Url.Action("Edit", "AdminVoucher", new { id = Model.VoucherID })" class="btn btn-outline-warning btn-sm">
                            <i class="fas fa-edit"></i>
                            Chỉnh sửa
                        </a>
                        <a href="@Url.Action("Delete", "AdminVoucher", new { id = Model.VoucherID })" class="btn btn-outline-danger btn-sm" onclick="return confirm('Bạn có chắc muốn xóa voucher này?');">
                            <i class="fas fa-trash"></i>
                            Xóa
                        </a>
                        <a href="@Url.Action("Index", "AdminVoucher")" class="btn btn-outline-secondary btn-sm">
                            <i class="fas fa-arrow-left"></i>
                            Quay lại danh sách
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
