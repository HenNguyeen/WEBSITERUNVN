@model DBStoreSport.Models.Voucher
@{
    ViewBag.Title = "Xóa Voucher";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Content/admin-dashboard.css" rel="stylesheet" />

<div class="admin-container">
    <div class="admin-header">
        <h2>🗑️ Xóa Voucher</h2>
        <a href="@Url.Action("Index", "AdminVoucher")" class="btn btn-outline-secondary btn-sm">
            <i class="fas fa-arrow-left"></i>
            Quay lại
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Xác nhận xóa Voucher
                    </h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-danger">
                        <h6><i class="fas fa-exclamation-triangle me-2"></i>Cảnh báo!</h6>
                        <p class="mb-0">Bạn sắp xóa voucher này. Hành động này không thể hoàn tác.</p>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-group mb-3">
                                <label class="info-label">ID Voucher:</label>
                                <div class="info-value">@Model.VoucherID</div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-group mb-3">
                                <label class="info-label">Mã Voucher:</label>
                                <div class="info-value">
                                    <span class="voucher-code">@Model.Code</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-group mb-3">
                                <label class="info-label">Loại giảm giá:</label>
                                <div class="info-value">
                                    @if (Model.Type == "Percent")
                                    {
                                        <span class="badge bg-primary">Phần trăm (%)</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning">Số tiền cố định (VNĐ)</span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-group mb-3">
                                <label class="info-label">Giá trị giảm:</label>
                                <div class="info-value">
                                    @if (Model.Type == "Percent")
                                    {
                                        <span class="badge bg-info">@Model.DiscountValue%</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success">@Model.DiscountValue.ToString("N0") VNĐ</span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="info-group mb-3">
                                <label class="info-label">Ngày hết hạn:</label>
                                <div class="info-value">
                                    @if (Model.ExpiryDate < DateTime.Now)
                                    {
                                        <span class="text-danger">
                                            <i class="fas fa-clock me-2"></i>
                                            @Model.ExpiryDate.ToString("dd/MM/yyyy")
                                            <br><small class="text-muted">Đã hết hạn</small>
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="text-success">
                                            <i class="fas fa-calendar-check me-2"></i>
                                            @Model.ExpiryDate.ToString("dd/MM/yyyy")
                                            <br><small class="text-muted">Còn hiệu lực</small>
                                        </span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-group mb-3">
                                <label class="info-label">Trạng thái sử dụng:</label>
                                <div class="info-value">
                                    @if (Model.IsUsed == true)
                                    {
                                        <span class="badge bg-secondary">
                                            <i class="fas fa-check-circle me-1"></i>
                                            Đã sử dụng
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success">
                                            <i class="fas fa-clock me-1"></i>
                                            Chưa sử dụng
                                        </span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">
                            <div class="info-group mb-4">
                                <label class="info-label">Trạng thái tổng thể:</label>
                                <div class="info-value">
                                    @if (Model.IsUsed == true)
                                    {
                                        <span class="badge bg-secondary fs-5">
                                            <i class="fas fa-ban me-2"></i>
                                            Voucher đã được sử dụng
                                        </span>
                                    }
                                    else if (Model.ExpiryDate < DateTime.Now)
                                    {
                                        <span class="badge bg-danger fs-5">
                                            <i class="fas fa-exclamation-triangle me-2"></i>
                                            Voucher đã hết hạn
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success fs-5">
                                            <i class="fas fa-check-circle me-2"></i>
                                            Voucher có hiệu lực
                                        </span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(model => model.VoucherID)

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="@Url.Action("Index", "AdminVoucher")" class="btn btn-outline-secondary btn-sm me-2">
                                <i class="fas fa-times"></i>
                                Hủy bỏ
                            </a>
                            <button type="submit" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash"></i>
                                Xác nhận xóa
                            </button>
                        </div>
                    }
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-header">
                    <h6 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>
                        Thông tin bổ sung
                    </h6>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <h6><i class="fas fa-exclamation-triangle me-2"></i>Lưu ý khi xóa:</h6>
                        <ul class="mb-0">
                            <li>Voucher sẽ bị xóa vĩnh viễn khỏi hệ thống</li>
                            <li>Không thể khôi phục voucher đã xóa</li>
                            <li>Nếu voucher đang được sử dụng, việc xóa có thể ảnh hưởng đến đơn hàng</li>
                            <li>Hãy đảm bảo rằng bạn thực sự muốn xóa voucher này</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border: none;
        border-radius: 16px;
        box-shadow: 0 2px 12px rgba(34,197,94,0.07);
    }

    .card.border-danger {
        border: 2px solid #dc3545 !important;
    }

    .card-header {
        border-radius: 16px 16px 0 0 !important;
        padding: 1rem 1.5rem;
    }

    .card-body {
        padding: 1.5rem;
    }

    .info-group {
        border-bottom: 1px solid #f1f5f9;
        padding-bottom: 1rem;
    }

    .info-group:last-child {
        border-bottom: none;
        padding-bottom: 0;
    }

    .info-label {
        font-weight: 700;
        color: #374151;
        font-size: 0.9rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.5rem;
    }

    .info-value {
        font-size: 1.1rem;
        color: #1f2937;
        font-weight: 500;
    }

    .voucher-code {
        font-family: 'Courier New', monospace;
        font-weight: bold;
        color: #22c55e;
        background: #f0fdf4;
        padding: 8px 12px;
        border-radius: 6px;
        border: 2px solid #bbf7d0;
        font-size: 1.2rem;
    }

    .badge {
        font-size: 0.85rem;
        padding: 8px 12px;
        border-radius: 6px;
    }

    .fs-5 {
        font-size: 1.1rem !important;
    }

    .alert {
        border-radius: 8px;
        border: none;
    }

    .alert ul {
        padding-left: 1.2rem;
    }

    .alert li {
        margin-bottom: 0.25rem;
    }

    .btn {
        border-radius: 8px;
        padding: 0.75rem 1.5rem;
        font-weight: 600;
        transition: all 0.2s;
    }
</style> 