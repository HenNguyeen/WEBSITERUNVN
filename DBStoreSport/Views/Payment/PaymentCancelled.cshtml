@{
    ViewBag.Title = "Hủy thanh toán";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-warning shadow">
                <div class="card-header bg-warning text-dark text-center">
                    <h3 class="mb-0">⚠️ Đã hủy thanh toán</h3>
                </div>
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="fas fa-ban text-warning" style="font-size: 4rem;"></i>
                    </div>
                    
                    <h4 class="text-warning mb-3">Bạn đã hủy giao dịch thanh toán</h4>
                    
                    @if (!string.IsNullOrEmpty(ViewBag.Message))
                    {
                        <div class="alert alert-success">
                            <p>@ViewBag.Message</p>
                        </div>
                    }
                    
                    <div class="alert alert-info">
                        <h6>📋 Thông tin:</h6>
                        <ul class="text-start">
                            <li>Đơn hàng của bạn vẫn được lưu với trạng thái "Chờ thanh toán"</li>
                            <li>Bạn có thể thanh toán lại bất kỳ lúc nào</li>
                            <li>Đơn hàng sẽ tự động hủy sau 24h nếu không thanh toán</li>
                            <li>Hoặc bạn có thể chọn thanh toán khi nhận hàng (COD)</li>
                        </ul>
                    </div>
                    
                    <div class="mt-4">
                        <a href="@Url.Action("ShowCart", "ShoppingCart")" class="btn btn-primary me-3">
                            <i class="fas fa-credit-card"></i> Thanh toán lại
                        </a>
                        <a href="@Url.Action("Index", "Home")" class="btn btn-outline-secondary">
                            <i class="fas fa-home"></i> Về trang chủ
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
